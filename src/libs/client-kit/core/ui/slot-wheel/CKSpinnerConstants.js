/**
 * States of a spinner
 * @enum
 */
ck.SpinnerState = {
    IDLE: 0,
    SPIN: 1,
};

/**
 * States of a spinner
 * @enum
 */
let SpinnerState = ck.SpinnerState;

/**
 * Spin state of a spinner <br/>
 * Used when a spinner is spinning <br/>
 * @enum
 */
ck.SpinnerSpinState = {
    SPEED_UP: 1,
    SPEED_MAX: 2,
    STOP: 3,
};

/**
 * Spin state of a spinner <br/>
 * Used when a spinner is spinning <br/>
 * @enum
 */
let SpinnerSpinState = ck.SpinnerSpinState;

/**
 * Wheel direction
 * @enum
 */
ck.WheelDirection = {
    RIGHT: 1,
    LEFT: -1,
};

/**
 * Wheel direction
 * @enum
 */
let WheelDirection = ck.WheelDirection;

/**
 * Wheel items direction
 * @enum
 */
ck.WheelItemsDirection = {
    RIGHT: 1,
    LEFT: -1,
};

/**
 * Wheel direction
 * @enum
 */
let WheelItemsDirection = ck.WheelItemsDirection;
